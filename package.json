{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "pnpm run build:libs && pnpm run typecheck && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run build",
    "build:libs": "tsc --build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "dependencies": {
    "@expo/cli": "54.0.23",
    "@expo/schema-utils": "^56.0.1",
    "typescript": "~5.9.3"
  },
  "devDependencies": {
    "@expo-google-fonts/inter": "^0.4.2",
    "@expo/vector-icons": "^15.1.1",
    "cross-env": "^7.0.3",
    "esbuild": "0.28.0",
    "prettier": "^3.8.3"
  },
  "overrides": {
    "esbuild": "0.28.0"
  }
}
